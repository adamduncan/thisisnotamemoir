/**
 * ES module entry. Add your logic here.
 * Runs in strict mode; top-level await supported in modern environments.
 * type="module" is deferred, so the DOM is ready when this runs.
 */

function init() {
    console.log('init')
}

init();
